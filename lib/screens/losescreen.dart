import 'package:flutter/material.dart';

class LoseScreen extends StatelessWidget {

  //This should be modified to take in a HangmanGame 
  LoseScreen();

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("You Lose"),);
  }
}